{
  "name": "zenfit-backend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "dev:local": "node --watch --env-file=.env.local src/server.js",
    "migrate": "node --env-file=.env scripts/migrate.js",
    "migrate:local": "node --env-file=.env.local scripts/migrate.js",
    "start": "node --env-file=.env src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "pg": "^8.13.1"
  }
}
